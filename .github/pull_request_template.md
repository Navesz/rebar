## What was wrong

<!-- The defect this change fixes, measured if you can: which repository, which rule, what it printed. -->

## What changes

## Checklist

- [ ] `npm run verify` passes locally
- [ ] a rule change keeps or adds a `pass`/`fail` proof case that differs in one thing
- [ ] generated files regenerated, not edited by hand (`node mcp/generate.mjs`, `node tooling/numbers.mjs`)
