{
  "regra": "conteudo-fora-do-codigo",
  "porque": "A §12.3 do plano fechou que o conteúdo do preset `site` mora em `conteudo/*.json`, e o próprio plano registra que fechar no texto não fecha nada. O lado `reprovar` é a MESMA árvore do `aprovar` com dois literais plantados no `.tsx` — um preço e uma frase de catálogo —, que é literalmente o par que a §12.3 pede.\n\nO que este caso passou a fazer, e não fazia: TRAVAR AS GUARDAS. A auditoria de 31/08 aplicou 32 mutações ao index.mjs e nove sobreviveram nesta regra, porque o `aprovar/` não exercitava nenhuma delas — ele nem continha `R$`. Agora cada guarda tem um espécime no lado que APROVA, e afrouxar a guarda pinta esse lado de vermelho:\n\n· `R$ {formatarPreco(...)}` — cifrão SEM dígito. É a forma certa: o símbolo é código, o valor vem de `conteudo/inicio.json` em centavos. Apagar a exigência de dígito do padrão de preço (`/R\\$\\s?\\d[\\d.,]*/` → `/R\\$/`) faz este lado reprovar.\n· `<p>Forma de pagamento</p>` — rótulo de campo de TRÊS palavras, medido no Galegos com essas mesmas palavras. Baixar o mínimo de palavras da frase faz este lado reprovar. É o limiar que substituiu o mínimo de 25 caracteres, que foi removido por custar 13 acusações verdadeiras nos 11 repositórios e não comprar nenhuma.\n· `<p>lote 7 · lote 9 · lote 12 · sem foto</p>` — 88% de letras, abaixo do mínimo. É linha de tabela renderizada como parágrafo, não prosa. Desligar `sinalDeCodigo` faz este lado reprovar.\n· `<button type=\"button\">Ver o catalogo completo</button>` e `<div>Nenhuma peca nesta colecao por enquanto.</div>` — quatro e seis palavras, os dois passariam por qualquer limiar de tamanho. O que os exclui é o DONO: `<button>` é controle e `<div>` não diz nada sobre o que carrega, e nenhum dos dois é elemento de prosa. Pôr `button` ou `div` em `PROSA` faz este lado reprovar. É o discriminador que tirou \"Imprimir ou salvar em PDF\" da acusação — ele mora num `<button>` em `decima-edicoes/app/components/print-button.tsx:8`.\n\nOs dois lados têm o mesmo `conteudo/inicio.json`, o mesmo formatador e o mesmo componente: a única diferença são os dois literais."
}
