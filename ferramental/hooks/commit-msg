#!/bin/sh
# Roda com o caminho do arquivo de mensagem em $1, antes de o commit existir.
# É a metade que faltava: o pre-commit checa o histórico, este checa o que está
# sendo escrito agora.
set -e
RAIZ=$(git rev-parse --show-toplevel)
exec node "$RAIZ/ferramental/hooks/checar-mensagem.mjs" "$1"
