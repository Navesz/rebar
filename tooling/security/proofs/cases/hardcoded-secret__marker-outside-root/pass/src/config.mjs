// The same folder with the same caso.json beside it, and the value read from
// the environment. A rule that failed any tree holding a caso.json fails here.
export const githubToken = process.env.GITHUB_TOKEN
