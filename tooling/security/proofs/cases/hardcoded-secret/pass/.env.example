# FALSO POSITIVO 6 — O ARQUIVO DE EXEMPLO, que documenta as variaveis sem os
# valores. E o CONSERTO da falha, e a forma sem aspas (`.env`, YAML, shell) e
# a que ja custou uma chave da AWS atravessar o gancho inteiro: sem aspas nao
# ha delimitador, entao o piso de tamanho sobe e quem segura o falso positivo
# e a lista de placeholder. Um valor com digito dentro exercita esse piso.
WEATHER_API_KEY=your_api_key_32_chars_here
DATABASE_URL=
PORT=
