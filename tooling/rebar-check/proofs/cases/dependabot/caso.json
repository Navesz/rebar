{
  "rule": "dependabot",
  "why": "An npm project with neither dependabot nor renovate piles up CVEs in a transitive dependency with nobody opening a PR. Both trees have package.json — without it the rule falls into not-applicable and the proof would prove nothing — and they differ only by .github/dependabot.yml."
}
