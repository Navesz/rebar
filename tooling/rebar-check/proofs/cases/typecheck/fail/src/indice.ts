// Mesmo arquivo do lado que aprova. A unica diferenca entre as duas arvores e o
// package.json: aqui o tipo so e conferido de carona no `build`, e e isso que a
// regra tem de pegar.
export function somar(a: number, b: number): number {
  return a + b
}
