{
  "rule": "typecheck",
  "why": "A TypeScript project whose only contact with the compiler is the build: the type error only shows up when someone tries to package, or in production, never in a check CI can call on its own."
}
