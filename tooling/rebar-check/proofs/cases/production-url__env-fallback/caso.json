{
  "rule": "production-url",
  "why": "Locks the false positive that opened this step, and it was measured: `decima-edicoes/app/lib/site.ts` line 5 writes `process.env.NEXT_PUBLIC_SITE_URL ?? 'https://navesz.github.io/decima-edicoes'` — an environment variable with a sensible default, which is EXACTLY what you want the person to write — and the rule accused it. The same pattern caught `hug-brasil-propostas` twice, in `scripts/check-access.js` and in `src/lib/accessControl.ts`. Three of the twelve accusations were the ruler hitting whoever got it right, and a rule that punishes correct behavior teaches people to switch the whole output off. Both sides have the SAME address in the SAME constant: only the fallback changes. Whoever deletes the defense makes the `pass` side go red."
}
