{
  "rule": "tests",
  "why": "The other half of the same false positive. This variant isolates the FILE NAME branch: the only test on the pass side is `provar-soma.mjs`, at the root, with no test folder in the path. It exists separately because the previous version of this proof had both branches in the same tree — and, restoring the Portuguese-blindness by hand, it stayed GREEN, because the other branch still matched. A proof that survives the return of the defect it locks is not a proof. `provar-` stays Portuguese: it is a NOME_TESTE alternative."
}
