{
  "rule": "ai-coauthorship",
  "why": "Both trees have the SAME file: only the history changes, so the proof measures exactly what the rule reads. The fail side uses the Cursor trailer in lower case (`Co-authored-by:`) because the naive regex looks only for `Claude` with a capital C and covers 15% of the measured problem; the pass side has a commit that CITES Claude and Copilot in the body, which is the false positive that shows up the moment somebody removes the `^` or the `/m` from the regex and it starts matching the bare word in the middle of the text.",
  "pass": {
    "commits": [
      {
        "mensagem": "cria o carregador de configuração",
        "autor": "Ana Prova <ana@rebar.local>"
      },
      {
        "mensagem": "documenta como rodar o rebar-check\n\nO passo a passo cita Claude Code e Copilot no corpo da mensagem.\nCitar a ferramenta não é assinar coautoria com ela: aqui não há trailer.",
        "autor": "Ana Prova <ana@rebar.local>"
      }
    ]
  },
  "fail": {
    "commits": [
      {
        "mensagem": "cria o carregador de configuração",
        "autor": "Ana Prova <ana@rebar.local>"
      },
      {
        "mensagem": "ajusta a leitura do arquivo de configuração\n\nCo-authored-by: Cursor Agent <cursoragent@cursor.com>",
        "autor": "Ana Prova <ana@rebar.local>"
      }
    ]
  }
}
