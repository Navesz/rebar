{
  "rule": "formatter",
  "why": "An npm project that has a linter and no formatter: each machine reindents the file its own way and the review diff becomes whitespace noise instead of a change of behavior."
}
