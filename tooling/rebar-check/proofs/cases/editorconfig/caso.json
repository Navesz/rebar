{
  "rule": "editorconfig",
  "why": "Without .editorconfig each editor rewrites indentation and line endings on its own, and in a Windows repository that becomes a CRLF diff in a file nobody touched. The two trees are identical except for that file: if the rule stops looking at it, the fail side exits 0 and the proof accuses."
}
