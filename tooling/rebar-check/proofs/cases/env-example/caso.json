{
  "rule": "env-example",
  "why": "An environment variable that the code reads and the .env.example does not list: whoever clones the repository only finds out it exists when the application breaks at runtime, and a half-done .env.example is worse than its absence because it looks complete."
}
