{
  "rule": "content-outside-code",
  "why": "§12.3 of the plan settled that the content of the `site` preset lives in `conteudo/*.json`, and the plan itself records that settling it in the text settles nothing. This case charges HALF of the pair §12.3 asks for — the SENTENCE. The price is the other case, `conteudo-fora-do-codigo__preco`, and the split is the fix of a measured defect, not tidying.\n\nWHY SPLIT: until 31/08 the `fail/` planted BOTH literals in the SAME `.tsx` — a sentence in a `<p>` and a price in a `<span>`. A single finding already turns the state into \"reprovou\", so the sentence carried the side alone and the price detection was never exercised: mutating `if (preco) achados.push(...)` into `if (false && preco) achados.push(...)` — the ENTIRE price detection erased — left the 51 proofs green. A proof that confuses two causes locks neither. Now the `fail/` of this case is the `pass/` with ONE literal planted, the catalog sentence in the `<p>`, and the printed reason has a single finding: `app/pagina.tsx: sentence \"Mesa de jantar em madeira macica, feita sob encomenda.\"`. Switching the sentence detection off (`if (false && frases.length)`) makes THIS side diverge.\n\nThe `pass/` locks the guards that decide what IS a sentence, and each one has a specimen that goes red if the guard is loosened:\n\n· `<p>Forma de pagamento</p>` — a THREE-word field label, measured in Galegos with those same words. Lowering `MIN_PALAVRAS` from 4 makes this side fail. It is the threshold that replaced the 25-character minimum, removed for costing 13 true accusations across the 11 repositories and buying none.\n\n· `<p>lote 7 · lote 9 · lote 12 · sem foto</p>` — 88% letters, below `MIN_LETRAS`. It is a table row rendered as a paragraph, not prose. Switching `sinalDeCodigo` off makes this side fail.\n\n· `<button type=\"button\">Ver o catalogo completo</button>` and `<div>Nenhuma peca nesta colecao por enquanto.</div>` — four and six words, both would clear any length threshold. What excludes them is the OWNER: `<button>` is a control and `<div>` says nothing about what it carries, and neither is in `PROSA`. Putting `button` or `div` into `PROSA` makes this side fail. It is the discriminator that took \"Imprimir ou salvar em PDF\" out of the accusation — it lives in a `<button>` at `decima-edicoes/app/components/print-button.tsx:8`. (The specimens above stay in Portuguese: they are the literal text of the fixture files, and the word counts the argument rests on are counts of THOSE words.)\n\nThe DIGIT guard in the price pattern left here together with the price: the `R$ {formatarPreco(...)}` specimen that locks it lives in the `pass/` of `conteudo-fora-do-codigo__preco`. Each case locks the guards of the cause it isolates, and no guard stays locked in two places.\n\nBoth sides have the same `conteudo/inicio.json` and the same component: the only difference is where the `<p>`'s text comes from, which is exactly the assertion of §12.3."
}
