{
  "rule": "orphan-schema",
  "why": "Sem nenhum `.schema.json` rastreado não há contrato para envelhecer em silêncio, e a regra não tem objeto. O ramo importa porque o lado positivo dela é uma busca por SUBSTRING no texto de todas as fontes: sem a guarda, um repositório sem schema nenhum passaria por um caminho que nunca foi exercitado, e \"passou\" ali é uma afirmação sobre zero arquivos. O lado `pass` tem código de verdade e nenhum schema; o lado `fail` tem `esquemas/pessoa.schema.json` que ninguém cita e continua reprovando.",
  "pass": {
    "estado": "na"
  }
}
