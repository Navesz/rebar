// The decision this file encodes came from the owner, and his words stay as
// he said them: "a decisao que mora onde nenhuma maquina le" [the decision
// that lives where no machine reads]. Translating a quote rewrites it.
//
// The sample below was measured in a Brazilian repository, so the Portuguese
// IS the measurement: "Nao deu para gerar o arquivo" is the shape this rule
// has to recognise. The user should read the configuration file for the rest.
export const alvo1 = { pronto: true }
