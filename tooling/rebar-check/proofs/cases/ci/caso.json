{
  "rule": "ci",
  "why": "A repository with no workflow at all has no gate: what gets through is whatever the author remembered to run on his own machine. The rule reads the list tracked by git, so the pass side also proves that a workflow only counts once committed — an untracked file does not count."
}
